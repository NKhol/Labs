@model IEnumerable<ClassLibrary.DIC_RANKS>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DRK_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DRK_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.DRK_ID  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.DRK_ID  })
        </td>
    </tr>
}

</table>
