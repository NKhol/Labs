@model IEnumerable<ClassLibrary.PERSON_AUDIENCE>

@{
    ViewBag.Title = "AllRelations";
}

<h2>AllRelations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Person
        </th>
        <th>
            Audience
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PERSON.PR_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIC_AUDIENCE.DAU_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.PAU_ID }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.PAU_ID })
        </td>
    </tr>
}

</table>
